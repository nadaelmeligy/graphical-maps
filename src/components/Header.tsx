'use client';
import { FC } from 'react';

const Header: FC = () => (
  <header className="bg-blue-600 text-white p-4 shadow">
    <h1 className="text-2xl font-semibold">3D Paper Network</h1>
  </header>
);

export default Header;